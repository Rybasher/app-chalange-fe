"use client"
import isAuth from "@/components/isAuth";

export default isAuth(function Collections() {

    return (
        <main className="flex min-h-screen flex-acol items-center justify-between p-24">
          Collections
        </main>
      );
});